@using Microsoft.AspNet.Identity
@if (this.User.IsInRole("Admin"))
{
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Administration<span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
            <li>@Html.ActionLink("User Infrormation", "Users", "Statistics", new { area = "Administration" }, new { @class = string.Empty })
            <li>
            <li class="divider"></li>
            <li>@Html.ActionLink("Total Information", "Total", "Statistics", new { area = "Administration" }, new { @class = string.Empty })</li>
        </ul>
    </li>
}